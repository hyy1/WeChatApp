<!--logs.wxml-->
<view class="container log-list">
  <block wx:for="{{plugins}}" wx:for-item="plugin" wx:key="*this">
    <text class="log-item">{{index + 1}}. {{plugin.name}}</text>
  </block>
</view>
